// ---------------------------------------------------------------------------
//  Fill these in with your Supabase project values, then commit + deploy.
//  Project settings > API  in the Supabase dashboard.
//
//  The anon key is SAFE to expose publicly — it only works alongside the
//  Row Level Security policies in supabase/schema.sql. Never put the
//  service_role key here; it belongs only in Netlify environment variables.
// ---------------------------------------------------------------------------
window.APP_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-ref.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-PUBLIC-ANON-KEY",
};
